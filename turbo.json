{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"],
      "cache": true
    },
    "run:tests": {
      "dependsOn": ["build"],
      "cache": true,
      "inputs": ["src/**/*.ts", "test/**/*.ts", "__tests__/**/*.ts"]
    }
  }
}
