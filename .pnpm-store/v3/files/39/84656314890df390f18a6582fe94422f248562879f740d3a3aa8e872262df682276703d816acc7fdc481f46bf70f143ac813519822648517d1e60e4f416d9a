import type { EngineConfig } from '../../common/Engine';
/**
 * Determine the client version to be sent to the DataProxy
 * @param config
 * @returns
 */
export declare function getClientVersion(config: EngineConfig): Promise<string>;
