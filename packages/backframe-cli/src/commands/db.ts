import { defineBfCommand } from "./index";

export default defineBfCommand({
  command: "db",
  description: "A utility function for db functionality",
  builder: (_) => _,
  handler: (_h) => {
    //
  },
});
